/*
 * SPDX-FileCopyrightText: 2025 The LineageOS Project
 * SPDX-License-Identifier: Apache-2.0
 */

package vendor.oplus.hardware.cameraMDM@2.0;

interface IOPlusCameraMDM {
    setCameraId(uint32_t cameraId, uint32_t logicalCameraId, uint32_t facing);
    setPackageName(string packageName);
    getPackageName() generates (string packageName);
    setJankMsg(string msg, uint32_t frameNumber, uint32_t timestamp);
    getJankMsg() generates (string msg);
    file_access(string path);
    file_delete(string path);
    file_open(string path);
    file_write(int32_t fd, vec<uint8_t> data, uint32_t size);
    file_read(int32_t fd, uint32_t size) generates (bool success, vec<uint8_t> data, uint32_t bytesRead);
    file_close(int32_t fd);
};
